神经网络灵感来源：人的神经
达到阈值->满足条件
分层：
- 输入层
- 隐藏层
- 输出层
隐藏层数＋输出层数=神经网络层数
$$

$$
$$
z = w^T x + b
$$
$$
a = \delta (z)
$$
$$
\delta(z) = \frac{1}{1 + e ^{-z}}
$$
计算图：
	分布，变量分离，运算简化
	链式运算求导法则，便于求偏导
___
激活函数：
$$
Tanh(x) = \frac{2}{1+e^{-2x}} -1
$$
$$
ReLU(x) = max(0, x)
$$
$$
Leaky ReLU(x) = max(0.1x, x)
$$
