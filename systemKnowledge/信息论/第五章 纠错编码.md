加入冗余之后，根据习惯经验得以进行检错与纠错。
线性分组码 - 群码
# 线性分组码
基本思路：将码字分成两部分：信息位；校验位
信息位：长度用k表示
校验位：也叫监督位，用于检错/纠错
码字：长度n表示
汉明重量：非零码元的个数，也叫码重
汉明距离：两个码字相应码元取不同数值的码元数，也叫码距
$$
d(c, c') = \sum_{i=1}^n c_i \oplus c_i'
$$
码的最小距离：
$$
d_{min} = \min d(c, c')
$$
码率：
$$
R = \frac{k}{n}
$$
