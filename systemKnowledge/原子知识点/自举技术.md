---
kind:
  - systemKnowledge
knowledgeKind:
  - computerScience
---
# 概述
自举技术用于解决密文计算过程中噪声积累的问题，以实现任意次数、任意复杂度的计算，是全同态加密的基础。
# 核心思想
同态加密时为了数据的安全性考虑，会在密文中加入“噪声”，而在加密运算的过程中噪声会逐步增长，当噪声超过一定的阈值之后解密就会失效。
自举的过程就是同态地解密，即在不解密密文的前提下，利用加密的私钥/公钥对当前的密文执行解密，输出一个新的，噪声更低的密文。